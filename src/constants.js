import axios from "axios"

export const SERVER_URL = process.env.REACT_APP_SERVER_URL

// export const getJwt = () => {
//     return JSON.parse(localStorage.getItem("items")).jwtToken
// }

// export let requestInstance = axios.create({
//   headers: {
//     Authorization: `Bearer ${JSON.parse(localStorage.getItem("items")).jwtToken}`,
//   },
// });
